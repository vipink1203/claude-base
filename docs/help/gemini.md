# Gemini Help

Use `/project-help` in Gemini CLI to load `.gemini/commands/project-help.toml`.

Gemini scaffold in this bootstrap:
- `GEMINI.md` for project context
- `.gemini/commands/project-help.toml` for product-specific help
- Optional `.gemini/agents/` when `--enable-experimental-subagents` is set
