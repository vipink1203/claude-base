# Claude Help

Use `/project-help` in Claude Code to display the project command guide from `.claude/commands/project-help.md`.

Recommended workflow:
1. Plan with BMAD agents (`analyst -> pm -> architect -> scrum-master`).
2. Implement in your main Claude session.
3. Run quality gate agents (`code-reviewer`, `security-reviewer`, `qa`, `ship`).
